spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/prices
    username: root
    password: pwd
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: validate

